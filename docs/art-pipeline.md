# 아트 파이프라인 (art-pipeline v0.1)

> 상태: **파이프라인 동작 확인, 히어로 1명 완료.** p001 서하율의 카드·썸네일이 게임에 들어가 있고,
> 화풍·모델·보정 방법이 실측으로 정해졌다(5.4·5.5). 남은 42명 중 41명은 아직 SVG 플레이스홀더다.
> 이 문서는 "그림 한 장을 어떻게 게임 안까지 넣는가"의 절차서다.
> 스타일·규격의 근거는 [art-style-guide.md](art-style-guide.md)에 있고, 여기서는 **손이 움직이는 순서**만 적는다.
>
> 코드: `tools/art-prompts.mjs`(프롬프트 생성) · `web/art-pack.mjs`(수집·규격 검사) · `web/build.mjs`(인라인) ·
> `web/art-test.mjs`(회귀). 산출물: `art/00_guide/prompts/`.

---

## 0. 한 장 요약

```
① node tools/art-prompts.mjs          → art/00_guide/prompts/p001.txt  (그대로 복사해 붙이는 프롬프트)
② 이미지 생성 도구에서 생성 → 8장 중 1장 선택
③ 리터치(누끼·번호·QA)                → art-style-guide 8절
④ art/04_export/p001/p001_card.webp + p001_thumb.webp 로 저장
⑤ node web/art-pack.mjs               → 규격 확인
⑥ node web/build.mjs                  → 카드가 게임 화면에 뜬다
```

**④번 폴더에 파일을 넣는 것 말고는 코드를 건드릴 일이 없다.** 아트가 없는 카드는 지금처럼 SVG 플레이스홀더가 그려지고,
한 명씩 들어와도 레이아웃이 흔들리지 않는다(같은 상자·같은 비율).

---

## 1. 프롬프트 팩 (`tools/art-prompts.mjs`)

`docs/art-style-guide.md` 6절의 템플릿은 `{hairStyle}` 같은 빈 슬롯이 뚫려 있고, `data/players.json` 의 외형 값은
`"롱 하이 포니테일"`·`"플래티넘 블론드"` 같은 한국어다. 이 둘을 잇는 것이 이 스크립트다.

```bash
node tools/art-prompts.mjs           # 42명분 생성 + 커버리지 리포트
node tools/art-prompts.mjs --check   # 파일을 쓰지 않고 검사만 (CI 게이트)
node tools/art-prompts.mjs --id p001 # 한 명만 표준출력으로
```

한 명당 나오는 것: **카드(태그형 + 자연어형) · 스탠딩 · SD 토큰 · 표정 4종 · 네거티브 2종**(카드/스탠딩 공용, 토큰 전용).
공용 고정 파라미터는 `_params.txt`, 전체 목록은 `_index.md`.

### 1.0 태그형과 자연어형

같은 매핑표에서 **두 가지 형태**를 뽑는다. 어휘를 두 벌 관리하지 않으려고 자연어는 태그에서 기계적으로 만든다.

| 형태 | 대상 | 예 |
|---|---|---|
| 태그형 | 로컬 SD · 애니메 SDXL(Danbooru 태그 학습) | `high ponytail, long hair, platinum blonde hair, purple eyes, tareme` |
| 자연어형 | Higgsfield(Soul·Seedream·Flux) · Midjourney 등 호스팅 모델 | `A tall, slim and athletic young woman with fair skin, long platinum blonde hair worn in a high ponytail and gentle, softly drooping purple eyes.` |

변환 규칙에서 주의한 것: 질감(straight/wavy/curly/layered)은 `with wavy hair` 가 아니라 **형용사로 앞에** 붙이고,
자른 머리(bob/pixie/hime/cropped)에는 길이 수식을 붙이지 않으며(`in a cropped hair` 방지),
눈 색과 눈매는 **한 덩어리**로 만든다(둘 다 `eyes` 로 끝나면 문장이 깨진다).
체형은 태그(`tall, slim athletic build`)가 명사구라 문장용 형용사구를 따로 둔다(`tall, slim and athletic`).
42종 헤어 전부 출력해 눈으로 확인했다.

### 1.1 어휘는 닫혀 있다

| 항목 | 값 개수 | 매핑 |
|---|---|---|
| 헤어스타일 | 42 | 42 ✅ |
| 헤어 컬러 | 24 | 24 ✅ |
| 눈 색 | 20 | 20 ✅ |
| 체형 | 15 | 15 ✅ |

런칭 42명과 **신인 세대 생성기**(`web/engine/rookies.js`)가 같은 목록에서 값을 뽑기 때문에,
이 표가 가득 차 있으면 앞으로 생길 카드도 전부 프롬프트를 만들 수 있다.
매핑에 없는 값이 하나라도 들어오면 스크립트가 **에러로 죽는다** — 조용히 빈 슬롯으로 넘어가지 않는다.

### 1.2 데이터에 없어 생성기가 배정하는 값

`art-style-guide` 는 요구하는데 `data/players.json` 에는 필드가 없는 것이 둘 있다. 생성기가 **결정적으로** 배정하고,
그 사실을 각 프롬프트 파일 머리말에 적는다.

| 값 | 규칙 | 실측 |
|---|---|---|
| 피부 3톤(2.7) | id 정렬 순서로 주기 배정 후, 단일 톤 구단이 없게 보정 | A/B/C = 26/12/4 (62/29/10%, 권장 60/30/10) · 구단별 최소 2톤 |
| 눈매(2.5·6.2) | id 순서 + 포지션으로 tsurime/neutral/tareme 순환 | — |

정식으로 하려면 `data/players.json` 스키마에 `skinTone`·`eyeShape` 를 넣고 생성기는 그것을 읽게 바꾸면 된다(11절).

### 1.3 프롬프트 조립 규칙

- **체형은 데이터의 `bodyType` 이 담당하고, 포지션은 그 포지션에서만 나오는 특징만 얹는다**(S `long fingers`,
  OP `long legs, broad shoulders`, MB `long arms`). 그러지 않으면 `장신 슬림 애슬레틱` + `athletic build` 처럼 같은 말이 겹쳐
  태그가 흐려진다.
- 태그 나열은 중복 제거를 거친다.
- **토큰(6.7)은 공용 네거티브를 쓰면 안 된다** — 공용 쪽에 `chibi` 가 들어 있어 서로 싸운다. 전용 네거티브를 따로 낸다.
- 리베로는 구단 대비색 유니폼으로 조립한다(5.4). 카드만 봐도 리베로여야 한다.
- **작가명·작품명·스튜디오명을 넣지 않는다.**

---

## 2. 파일을 넣는 곳 (`art/04_export/`)

```
art/04_export/{pid}/
  {pid}_card.webp      3:4   (권장 2048×2732)   카드 일러스트
  {pid}_thumb.webp     1:1   (권장 512×512)     원형 초상 — 로스터·라인업·육성 목록
  {pid}_meta.json                                얼굴 좌표 등. 없어도 동작한다
```

- `webp` 가 `png` 보다 우선한다(같은 화질에 30~40% 크기). `png`·`jpg` 도 읽는다.
- **비율만 검사하고 해상도는 강제하지 않는다.** 작업 중에 낮은 해상도로 먼저 확인하는 편이 낫기 때문이다.
- 폴더 이름과 파일 접두사는 `data/players.json` 의 `id` 와 같아야 한다(`p001`…). 신인 세대 카드는
  런타임 생성이라 아트를 붙일 수 없다 — 항상 SVG 플레이스홀더다(11절 열린 이슈).

원본(`02_gen`)·작업 파일(`03_work`)·LoRA 데이터셋(`06_lora`)은 용량이 커서 저장소에 넣지 않는다.
**`04_export` 만 커밋한다**(art-style-guide 4.9).

```bash
node web/art-pack.mjs        # 지금 무엇이 들어와 있는지 · 비율이 맞는지 · 합계 용량
```

---

## 3. 게임에 들어가는 방식

빌드가 `04_export` 를 훑어 **data: URI 로 HTML 안에 인라인**한다(`window.BLOOM_ART`).

왜 인라인인가: 산출물이 단일 HTML 한 장이고, 아티팩트로 게시하면 외부 이미지가 CSP 로 막힌다.
휴대폰에 파일 하나만 건네도 그림이 보여야 한다.

```bash
node web/build.mjs
# web/dist/bloom.html  484KB  (아트 없음 — SVG 플레이스홀더)
# web/dist/bloom.html  6.2MB  (아트 12명 · 5.71MB)          ← 그림이 들어오면
```

**용량 예산 11MB**(`ART_BUDGET_MB`). 아티팩트 상한 16MB 에 여유를 둔 값이고, 넘으면 빌드가 경고한다.
42명 전원의 카드+썸네일을 넣을 것이므로 **카드 1장을 200KB 안쪽으로** 잡아야 한다(webp 품질 80 · 긴 변 1400px 정도).
원본 2048×2732 PNG 는 저장소·Unity 용이고, 웹 프로토타입에는 축소본을 넣는다.

화면 쪽은 `portraitSvg()` / `cardArtSvg()` 가 아트를 먼저 찾고 없으면 SVG 로 떨어진다. 둘은 같은 상자를 쓴다.

---

## 4. 검증

```bash
node tools/art-prompts.mjs --check   # 어휘 커버리지 · 피부톤 분포 규칙
node web/art-pack.mjs                # 들어온 파일의 비율·용량
node web/art-test.mjs                # 슬롯인 회귀 12건 (playwright 필요)
node web/app-test.mjs                # 화면 흐름 32건 — 아트가 없는 상태를 검사한다
```

`art-test.mjs` 는 검사용 단색 이미지를 42명분 잠깐 넣고 빌드해 **실제 화면에서 `<img>` 로 그려지고 디코딩까지 되는지**
보고, 끝나면 지운다. 아트 경로를 일부러 끊는 두 가지 변이(`artOf` 가 항상 null / 빌드가 인라인 생략)를 넣어
각각 4건·5건이 실패하는 것을 확인했다.

`app-test.mjs` 32건이 아트가 **하나도 없는** 상태로 도는 것이 곧 SVG 폴백 검사다.

---

## 5. 히어로 시험 제작 (다음 할 일)

`art-style-guide` 9절의 절차를 이 저장소 기준으로 다시 적는다.

| 단계 | 할 일 | 완료 조건 |
|---|---|---|
| 1 | 체크포인트 1개 선택, `_params.txt` 에 이름·해시 기록. 라이선스 확인(10절) | 상용 이용 가능 여부가 문서에 남는다 |
| 2 | `{quality_prefix}` 를 그 모델의 권장 품질 태그로 확정 | `_params.txt` 갱신 |
| 3 | 포즈 뱅크 5종(포지션별 1개) 확보 — 셀프 촬영 또는 3D 포즈 툴(3.4절 A·B) | `art/05_shared/pose/` 에 OpenPose JSON |
| 4 | **히어로 3명**을 끝까지 만든다. 포지션·구단·등급이 갈리게: p001(OH·SSR·t01) · p007(L·R·t01) · MB 한 명 | `04_export` 에 카드+썸네일, `art-pack` 규격 통과 |
| 5 | 휴대폰에서 확인 — 가챠 결과·로스터·라인업에서 어떻게 보이는가 | "이 그림이면 뽑고 싶은가"에 답이 나온다 |
| 6 | 태그 하나씩 빼 보며(ablation) 실제로 듣는 태그만 남겨 템플릿 확정 | 생성기의 매핑표를 고쳐 재생성 |

**3명을 먼저 하는 이유**: 1명으로는 "42명이 같은 스타일로 보이는가"(8.2절 편차 QA)를 판정할 수 없고,
42명을 만들고 나서 스타일이 갈린 것을 발견하면 전부 다시 해야 한다.

### 5.1 원본 → 규격 변환 (`tools/art-import.mjs`)

생성 도구가 내주는 비율(2:3 등)과 용량(2MB)은 카드 규격이 아니다. 이 스크립트가 4.2·4.3 규칙대로 잘라 넣는다.

```bash
node tools/art-import.mjs p001 원본.png --face 470,310,180   # 얼굴 중심 x,y 와 얼굴 높이(원본 픽셀)
node tools/art-import.mjs p001 원본.png --face 470,310,180 --dry   # 계산만 보고 파일은 안 씀
```

- 카드는 3:4 로, **얼굴 중심이 세로 30% 에 오도록** 맞추고 원본을 벗어나면 경계로 민다.
- 썸네일은 한 변 = 2.2 × 얼굴 높이, 중심 = (cx, cy + 0.15 × 얼굴 높이).
- WebP 로 인코딩하며 **카드 200KB · 썸네일 60KB** 예산에 들 때까지 품질을 낮춘다.
- 카드 좌표계로 환산한 얼굴 위치를 `{pid}_meta.json` 에 남긴다.
- 이 환경에는 ImageMagick·Pillow·sharp 가 없어 **이미 있는 Chromium 의 캔버스**로 처리한다. 새 의존성이 없다.

### 5.2 히어로 시험 1호 (p001 서하율) — 결과

첫 실물 카드를 넣어 보고 얻은 것. **프롬프트를 고친 근거가 전부 여기서 나왔다.**

맞은 것: 헤어(플래티넘 블론드 롱 하이 포니테일)·눈(바이올렛)·유니폼 색(t01 바이올렛+사이언)·빈 유니폼(5.2)·
무릎보호대·양말·배구화·스파이크 포즈·로우앵글 포어쇼트닝·체육관과 네트·SSR 조명. **스타일 자체는 목표에 근접했다.**

고쳐야 하는 것:

| # | 문제 | 근거 | 대응 |
|---|---|---|---|
| 1 | 원본이 2:3(1024×1536) | 규격 3:4 | 생성 단계에서 3:4 로 뽑는다. 지금은 하단 171px 을 잘라 맞췄다 |
| 2 | **카드 96px 에서 얼굴이 안 읽힌다** | 얼굴이 (46%, 23%) 에 있고 머리 높이가 프레임의 13% — 4.2 는 (50%, 30%)±(6,5) 에 머리 높이 18~23% 를 요구 | 자연어 프롬프트에 프레이밍 문장을 명시 |
| 3 | 프레임 중앙이 엉덩이·허벅지 | 1.4 "엉덩이·가슴 중심 프레이밍 금지" | 위와 같음 + 네거티브에 `ass focus, hip focus` |
| 4 | 쇼츠가 짧고 밀착 | 1.4 "밑단 = 사타구니→무릎의 1/3 이상 아래", "밀착 부위 형태 강조" 금지 | 포지티브에 쇼츠 길이·루즈핏 문장, 네거티브에 `skin tight shorts, short shorts` |
| 5 | 가슴 과장 | 1.4 "가슴 과장" 금지 | 포지티브 `loose fit jersey, modest sportswear` |
| 6 | 허벅지에 흐르는 땀 | 1.4 "땀은 얼굴·목·팔에 소량 방울로만" | 네거티브 `wet thighs, sweat on thighs` |
| 7 | **공이 파랑/노랑/흰 18패널** | 9.x "실제 배구공 브랜드 색 배열 금지" · 4.8 공식구는 흰 `#F4F5F8` + 민트 `#3EC1B3` + 코랄 `#FF6B57` | 자연어에 공 색 문장 추가, 네거티브 `branded volleyball` |
| 8 | 원본 2MB | 예산 카드 200KB | `art-import.mjs` 가 WebP 98KB 로 변환(42명 ≈ 5.3MB, 예산 11MB 안) |

**호스팅 모델은 네거티브를 안 받을 수 있어 포지티브가 방어선이다** — 그래서 1.4 의 금지선을 포지티브 문장으로도 못박았다.

### 5.3 히어로 시험 2호 — Higgsfield MCP 로 직접 생성

커넥터가 붙어 세션 안에서 바로 생성했다. `nano_banana_pro`(실제 실행은 `nano_banana_2`), 3:4, 2k, 4장 2 크레딧.
카탈로그에 애니메 전용 모델이 없어 **프롬프트 준수력**으로 골랐다.

**프롬프트로 고쳐진 것** (1호의 8건 중 6건):

| 1호 문제 | 2호 결과 |
|---|---|
| 프레임 중앙이 엉덩이 | ✅ 상반신 중심 |
| 쇼츠 짧고 밀착 | ✅ 표준 배구 쇼츠 |
| 가슴 과장 | ✅ 해소 |
| 허벅지 땀 | ✅ 없음 |
| 공이 실제 브랜드 색 배열 | ✅ 흰+민트+코랄로 나온다 |
| 비율 2:3 | ✅ 3:4(1792×2400) |

**2호에서 새로 나온 것과 대응:**

- **한 장에 "SUPER SPIKE" 글자가 박혔다.** 모델이 "trading card" 라는 말에 제목을 얹으려 한다.
  → 텍스트 금지를 **맨 앞 문장**으로 올렸다.
- **두 장에서 배꼽이 드러났다**(1.4 위반). → "밑단이 쇼츠에 들어가 있고 배는 완전히 가려진다"를 명시.
- **신발에 브랜드 마크 같은 무늬.** → `plain white volleyball shoes with no markings`.

**프롬프트로 안 고쳐지는 것 하나 — 얼굴 크기.**

6장 전부 머리가 프레임의 **10~16%** 로 나왔다. 4.2 규격은 18~23% 다.
"머리가 화면 높이의 1/5", "head-and-torso hero shot", "전신을 잡지 마라"를 다 넣어도 모델은 전신을 그린다.
크레딧을 더 쓰며 프롬프트를 다투는 대신 **크롭으로 맞춘다**:

```bash
node tools/art-import.mjs p001 원본.png --face 700,490,240 --headfrac 0.20
```

`--headfrac` 는 "카드 높이의 몇 %를 머리가 차지할지"를 받아 그 크기가 되도록 3:4 를 잘라 낸다.
결과: 얼굴 위치 (50%, 30%) ✅ · 머리 크기 20% ✅ · 카드 85KB · 썸네일 38KB.
**이게 파이프라인의 결론이다 — 생성은 소재를 만들고, 규격은 크롭이 맞춘다.**

남은 것: 배경 관중이 2.8 절의 보케보다 또렷하고, 1호와 2호의 화풍이 다르다(8.2 편차 QA는 같은 모델·같은 파라미터로
3명을 만든 뒤에 판정해야 한다).

### 5.4 히어로 시험 3호 — **화풍은 프롬프트보다 모델이 결정한다**

2호를 본 사용자의 평가: *"처음 내가 만든 이미지가 더 좋은데 지금은 초등학교 만화 같다."* 맞는 지적이었고, 원인은 두 개다.

**① 스타일 문구가 납작함을 요구하고 있었다.** 자연어 프롬프트에 `flat vivid colors, clean lineart, cel shading` 이 들어 있었다.
이건 태그형에서 가져온 문구인데, 태그형은 **애니메 특화 SDXL** 을 전제한다 — 거기서 이 태그들은 "애니메 화풍"이라는 뜻이다.
**범용 모델은 같은 단어를 문자 그대로 받는다.**

**② 모델이 애니메용이 아니었다.** Nano Banana(Google)는 범용·실사 지향이다.

| | 2호 (Nano Banana + flat 문구) | 3호 (Seedream 4.5 + 밀도 문구) |
|---|---|---|
| 셰이딩 | 단색 2단, 굵고 균일한 외곽선 | 에어브러시 계조, 선 굵기 변화 |
| 머리카락 | 덩어리 | 가닥별 스페큘러 |
| 눈 | 하이라이트 1개 | 다중 하이라이트, 발광 |
| 조명 | 평면 | 림라이트·블룸·플레어·보케 |

바꾼 문구(자연어형에만 적용, 태그형은 그대로 둔다):

```
Premium mobile gacha game character card illustration — high-detail anime key visual,
semi-realistic anime rendering with soft airbrushed shading, glossy specular highlights,
individually rendered hair strands, luminous skin, rich colour depth, delicate varying line weight,
large expressive eyes with multiple highlights, subtle bloom and lens flare.
NOT flat colours, NOT simple cartoon, NOT thick uniform outlines, NOT a children's comic.
```

**확정**: 자연어형은 **Seedream 4.5**(`quality: high`, 3:4). Nano Banana 계열은 쓰지 않는다.

### 5.5 프롬프트로 안 잡히면 **편집 패스**로 고친다

Seedream 4장이 4장 모두 배꼽을 드러냈다(1.4 위반). 대문자로 못박아도 마찬가지였다.
프롬프트를 계속 다투는 대신, **생성된 그림을 그대로 참조로 넣고 옷만 고치게** 했다:

```
model: seedream_v5_pro
medias: [{ value: <media_id>, role: 'image_references' }]
prompt: "Edit this illustration: make the violet jersey longer so its hem overlaps the top of the
         shorts, completely covering her stomach — no bare skin. Keep everything else exactly as it is:
         same character, face, pose, ball, background, lighting and art style."
```

원본 URL 은 `media_import_url` 로 `media_id` 를 받아 넘긴다. 얼굴·포즈·조명·화풍이 유지된 채 옷만 바뀐다.
**42명에 쓸 수 있는 수법이고, 리터치 공정(8절)의 일부를 자동화한다.**

이렇게 해서 p001 최종본은 규격을 전부 통과한다 — 얼굴 (50%, 30%) · 머리 20% · 카드 94KB · 썸네일 45KB ·
글자 없음 · 민무늬 신발 · 공식구 색 · 배 가림.

### 5.6 지금까지의 크레딧

| 라운드 | 모델 | 장수 | 크레딧 |
|---|---|---|---|
| 1~2 | nano_banana_pro | 8 | 4 |
| 3~4 | seedream_v4_5 (high) | 8 | 2 |
| 보정 | seedream_v5_pro (편집) | 2 | 1 |
| **합계** | | **18** | **7** |

카드 한 장을 확정하는 데 약 2크레딧. 42명이면 **100크레딧 내외**로 추정된다.

---

## 6. 열린 이슈

- [ ] **헤어 컬러 "선셋 오렌지" 가 스타일 가이드 2.7 의 12색 팔레트에 없다.** p025 심노을이 쓴다.
      팔레트에 오렌지를 추가하거나 데이터 값을 바꿔야 리터치 색을 정할 수 있다.
- [ ] **같은 구단 안에서 팔레트 색이 겹치는 조합 3건.** `world.md` 6.2 는 "같은 구단 안 헤어 컬러 중복 없음"이라고
      하지만, 이름이 다른 색이 12색 팔레트에서는 같은 칸으로 수렴한다.
      - 태령 피크스 #6: 진로아(애쉬 브라운) · 반소리(애쉬 그레이)
      - 연화 크레인즈 #6: 현아린(애쉬 브라운) · 우로운(차콜 그레이)
      - 라온 브리즈 #4: 공산들(골든 브라운) · 설하루(라이트 브라운)

      팔레트를 세분하거나(12 → 16색), 데이터 값을 바꾸거나, "팔레트는 같아도 명도·하이라이트로 구분한다"를
      규칙으로 명시하는 세 가지 중 하나를 정해야 한다.

      > **경과**: 이 결정을 못 내린 채 42장을 만들었다(13절). 생성 모델은 팔레트가 아니라 색 이름
      > ("애쉬 브라운" vs "애쉬 그레이")을 읽으므로 **여섯 조합 모두 그림에서는 구분된다** — 44px 초상에서도
      > 갈린다. 즉 지금 당장 깨진 것은 없고, 남은 것은 리터치·LoRA 단계에서 색을 못 박을 때의 문제다.
      > 그때 세 안 중 하나를 고르면 된다.
- [ ] **피부톤·눈매가 데이터에 없다.** 지금은 생성기가 결정적으로 배정한다. 스키마에 넣으면 캐릭터 설정과
      그림이 한 곳에서 관리된다.
- [ ] **신인 세대 카드는 아트를 가질 수 없다.** 런타임 생성이라 `pid` 가 저장소에 없다. 시즌 4 이후 스카우트 목록의
      절반 이상이 신인이므로, 장기적으로는 ① 제네릭 N 아트 몇 종을 외형 값으로 조합하거나 ② 신인도 미리 만들어 둔
      풀에서 뽑게 바꿔야 한다.
- [x] **웹 프로토타입 인라인 용량.** ~~42명 × (카드+썸네일)을 예산 11MB 안에 넣으려면 카드 1장 200KB 가 상한이다.~~
      **해결.** 실측 42명 5.78MB(1인당 평균 141KB) — 예산 11MB, 아티팩트 상한 16MB 안이다.
      정식 앱(Unity)은 Addressables 원격 그룹이라 이 제약이 애초에 없다.
- [ ] AI 생성 고지·라이선스(art-style-guide 10절)는 여전히 미결. 체크포인트를 고르는 순간 결론이 필요하다.


---

## 7. 등급 상향 (v0.2) — 규정이 그림을 죽이고 있었다

히어로 시험 3호까지 오고 나서 드러난 것: **12세 금지선이 경기 장면의 박진감까지 같이 잘라 내고 있었다.**
"엉덩이·가슴 중심 프레이밍 금지", "땀은 얼굴·목·팔에만", "밀착 부위 형태 강조 금지", "배꼽 노출 금지" 를
프롬프트에 그대로 옮기면 모델은 **동작 자체를 밋밋하게** 그린다 — 점프가 낮아지고, 근육이 사라지고,
땀과 흐름이 없어진다. 규정을 지킨 결과가 "배구 카드처럼 안 보이는 배구 카드"였다.

`art-style-guide.md` 1.4 를 다시 썼다.

- **목표 등급 12세 → 15세.** 한 번 청소년이용불가까지 열기로 했다가 되돌렸다 —
  실제로 원한 것이 "경기의 박진감과 선수의 매력"이었고 그건 15세 안에서 전부 되며,
  19세는 원하지 않는 것(나체·성행위)을 허용하는 대신 심의·스토어·광고 제약만 남기기 때문이다(1.4.3).
- **1.4.1 을 신설했다 — 금지선이 아니라 요구 사항이다.** 높이·힘·긴장·속도·동작에서 나오는 실루엣·
  몸에 맞는 유니폼·전신의 땀·아끼지 않는 조명. **이게 없으면 QA 탈락**이다.
- 금지선(1.4.2)은 나체·속옷·과도한 노출·성행위·투시·업스커트·부위 클로즈업·아동형 체형으로 좁혔고,
  **실존 브랜드와 화면 내 문자는 등급과 무관하게 유지**한다.

프롬프트 생성기도 같이 바꿨다. `Modest sports illustration, no emphasis on chest, hips or thighs` 와
`loose-fitting`·`midriff covered` 문장을 걷어내고, 1.4.1 을 한 문장으로 요구한다:

```
Give it the intensity of a real match: she is at the very top of her jump with her feet clearly off the
floor, the low angle exaggerating her height and reach; an athlete's muscle definition visible in her
arms, shoulders and thighs; a fierce competitive expression with her eyes locked on the ball; hair,
jersey and beads of sweat all streaming in the direction of the motion.
```

네거티브에서도 `ass focus, thigh focus, hip focus, breast focus, skin tight shorts, wet thighs` 를 뺐다 —
이것들이 박진감을 죽이던 항목이다. `nsfw, nude, underwear, see-through, upskirt` 는 남긴다.

**결과**: 같은 모델·같은 크롭 규칙으로 도약·근육·땀·시선이 살아난 카드가 나왔다. 규격도 전부 통과
(얼굴 (50%, 30%) · 머리 20% · 카드 98KB · 썸네일 36KB).

### 7.1 남은 QA 항목

- **머리 모양이 데이터와 다르다.** p001 은 `롱 하이 포니테일`(하나)인데 생성물은 트윈테일이다.
  헤어스타일은 42명 구분의 1축(world.md 6.2)이라 그냥 넘길 수 없다 — 생성 시 강조하거나 편집 패스로 고친다.
- 배경 관중이 2.8 절의 보케보다 또렷하다.
- 화풍 편차 판정(8.2)은 같은 모델·같은 파라미터로 히어로 3명을 만든 뒤에 한다.


---

## 8. 화풍 확정 (v0.3) — "더 리얼하게"

15세 규정판까지 와서 나온 평가: *"더 디테일해야 할 것 같은데 부족해. 더 리얼하게 그렸으면 좋겠어."*
스타일 문구를 **묘사 밀도** 쪽으로 한 번 더 밀었다.

| 판 | 스타일 문구 | 결과 |
|---|---|---|
| v0.1 | `flat vivid colors, clean lineart, cel shading` | 납작한 만화 |
| v0.2 | `soft airbrushed shading, glossy highlights, per-strand hair, luminous skin` | 가챠 카드 수준 |
| **v0.3** | **`rendering pushed close to realism` — 피부 텍스처·subsurface·가닥별 헤어·직물의 짜임과 봉제선·해부학적 근육·손가락 힘줄·개별 땀방울·볼류메트릭 조명** | 반실사 키비주얼 |

모델도 `seedream_v4_5` → **`seedream_v5_pro`**(2k)로 올렸다.

### 8.1 편집 패스의 한계 — 옷은 고치고 머리는 못 고친다

p001 의 헤어스타일이 데이터(`롱 하이 포니테일`)와 달리 트윈테일로 나와서 편집 패스로 고치려 했으나
**4장 모두 실패**했다. 옷의 마크 제거·밑단 연장은 되는데, 헤어스타일은 **구조 변경**이라 안 된다.

| 수정 종류 | 편집 패스 | 재생성 |
|---|---|---|
| 옷의 로고·마크 제거 | ✅ | — |
| 상의 밑단 길이 | ✅ | — |
| 색 교체 | ✅(미검증) | — |
| **헤어스타일** | ❌ | ✅ |
| 포즈·구도 | ❌ | ✅ |

그래서 생성기의 자연어 프롬프트에서 헤어를 **대문자 전용 문장**으로 분리했다:
`HER HAIR: long platinum blonde hair worn in a high ponytail — exactly this and nothing else.`
재생성 한 번에 단일 포니테일이 나왔다.

**규칙**: 헤어·포즈가 틀리면 재생성, 옷·마크가 틀리면 편집 패스. 편집이 싼데 안 되는 것이 있다.


---

## 9. 미디엄 샷 — 크롭 손실을 없앤다 (v0.4)

전신 샷은 8라운드 내내 머리가 프레임의 **10~16%** 로 나왔다. 4.2 규격은 18~23% 이고,
`--headfrac` 로 잘라 맞출 수는 있지만 **원본의 일부만 쓰게 되어 해상도가 깎인다**.

프롬프트 맨 앞에 프레이밍을 못박으니 해결됐다:

```
MEDIUM SHOT / WAIST-UP CROP. The bottom edge of the frame cuts her off at mid-thigh —
her lower legs and feet are NOT in the picture. Her head, shoulders and torso fill most
of the frame; her head alone is about one fifth of the total image height.
This is a close hero portrait, not a full-body shot.
```

| | 전신 샷 (v0.3) | 미디엄 샷 (v0.4) |
|---|---|---|
| 원본에서 머리 크기 | 11% | **19.6%** |
| 규격(18~23%) | ✗ | **✅ 크롭 전에 이미 통과** |
| 크롭이 쓰는 원본 면적 | 약 30% | **약 96%** |
| 카드 해상도 손실 | 있음 | **거의 없음** |

**"화면의 1/5" 같은 비율 지시는 안 먹히고, "무릎 아래는 프레임 밖" 같은 물리적 지시는 먹힌다.**
모델은 비율을 못 세지만 무엇을 자를지는 안다.

세트 프롬프트 순서도 중요했다 — 프레이밍 문장을 **맨 앞**에 두어야 한다. 스타일·인물 묘사 뒤에 두면 묻힌다.


---

## 10. 캐릭터당 그림 두 장 — 카드와 전신

미디엄 샷과 전신 중 하나를 고르는 대신 **둘 다 쓴다.** 쓰이는 자리가 다르다.

| 슬롯 | 파일 | 프레이밍 | 쓰이는 곳 |
|---|---|---|---|
| 카드 | `{pid}_card.webp` | **미디엄 샷**(허벅지 컷) | 스카우트 결과·리스트. 96px 로 줄어도 얼굴이 읽혀야 한다 |
| 썸네일 | `{pid}_thumb.webp` | 얼굴 크롭(카드에서 자동) | 로스터·라인업·육성 목록의 원형 초상 44px |
| 전신 | `{pid}_hero.webp` | **전신 액션** | 선수 상세 화면 상단 전체 폭 배너. 점프의 박력을 크게 본다 |

프롬프트 팩은 캐릭터당 **CARD 와 HERO 두 문단**을 낸다. 차이는 **맨 앞 프레이밍 문장 하나**뿐이고
나머지(화풍·인물·유니폼·조명·등급 규정)는 완전히 같다 — 그래야 두 장이 같은 캐릭터로 보인다.

```bash
node tools/art-import.mjs p001 medium.png --face 897,732,460 --headfrac 0.20   # 카드 + 썸네일
node tools/art-import.mjs p001 full.png   --kind hero                          # 전신 (자르지 않는다)
```

전신은 크롭하지 않는다 — 화면에서 전체를 보는 그림이라 얼굴 위치를 맞출 이유가 없다.

**용량**: 1인당 카드 97KB + 썸네일 41KB + 전신 97KB = 235KB → **42명이면 약 9.6MB**, 예산 11MB 안.
`node web/art-pack.mjs` 가 지금 넣은 것의 1인당 평균으로 42명 추정치를 같이 찍는다 —
다 만들고 나서 예산 초과를 발견하는 일이 없도록.

**화면**: `heroBanner()` 가 전신을 전체 폭 3:4 로 깔고 아래를 배경색으로 그라데이션 페이드한다.
전신이 없는 카드는 지금까지처럼 작은 카드 아트가 나온다 — 41명이 아직 그 상태이고, 한 명씩 들어와도 화면이 깨지지 않는다.


---

## 11. 히어로 3명 — 편차 QA 결과 (8.2절)

포지션·등급·구단이 갈리는 셋으로 "42명이 한 화풍으로 보이는가"를 판정했다.
같은 모델(`seedream_v5_pro`)·같은 설정(3:4 · 2k)·**같은 프롬프트 뼈대**로 뽑았다.

| pid | 이름 | 포지션 | 등급 | 구단 | 헤어 | 유니폼 |
|---|---|---|---|---|---|---|
| p001 | 서하율 | OH | SSR | 가온(t01) | 플래티넘 블론드 하이 포니테일 | 바이올렛 + 사이언 |
| p007 | 문반디 | L | R | 가온(t01) | 브라운 사이드 브레이드 | **시안 리베로** + 바이올렛 |
| p023 | 방한결 | MB | SSR | 적동(t04) | 블랙 크롭 숏컷 | 크림슨 + 카퍼 |

**통과한 것**

- **화풍이 갈리지 않는다.** 묘사 밀도·선·셰이딩·공 디자인·조명 처리가 셋 다 같은 계열로 읽힌다.
- **44px 원형 초상에서 세 명이 구분된다** — 헤어 색과 유니폼 색이 실루엣 없이도 갈린다.
- **리베로 규칙이 그림에 반영됐다**(5.4). 문반디만 팀 대비색(시안)이라 카드만 봐도 리베로다.
- 포지션별 포즈가 각각 스파이크·디그·블로킹으로 나와 역할이 읽힌다.

**드러난 문제**

| # | 문제 | 원인 | 대응 |
|---|---|---|---|
| 1 | **배경 명도가 크게 갈린다.** 문반디(R)는 밝은 주간 체육관, 나머지 둘(SSR)은 어두운 야간 아레나 | 등급별 조명 차등(6.4)이 **의도대로** 작동한 결과 — R `soft gym lighting` vs SSR `dramatic rim light + spotlight` | 등급 차등은 유지하되 **배경 명도 범위를 좁힐지** 결정 필요. 리스트에 섞이면 R 카드만 흰 판처럼 튄다 |
| 2 | 문반디의 머리가 프레임의 **31%**(규격 18~23%) | 디그는 몸이 낮고 카메라가 가까워 자연히 얼굴이 커진다. 크롭으로는 **줄일 수 없다**(화면 밖을 만들 수 없다) | 리베로·세터처럼 낮은 자세 포즈는 프롬프트에 거리를 더 주거나, 규격 밴드를 포지션별로 다르게 잡는다 |
| 3 | 방한결이 민소매 | 프롬프트는 `short sleeves` | 경미. 리터치 또는 편집 패스 |

**결론**: 화풍 편차는 없다. 42명으로 확대해도 된다.
다만 **① 등급별 배경 명도**와 **② 낮은 자세 포지션의 얼굴 크기**는 42명을 만들기 전에 정하는 편이 싸다.

## 12. 16명 투입 (v0.5) — **머리 크기는 프롬프트가 아니라 크롭이 맞춘다**

11절이 남긴 두 결정을 실제 16장으로 확정했다.

### 12.1 배경 명도 (11절 문제 ①)

등급별 조명 차등은 **효과만** 남기고 주변 명도는 전 등급 공통으로 못 박았다.
모든 카드가 "야간의 어두운 아레나"를 명시하고, 등급은 림 라이트·스포트라이트·렌즈 플레어·
광입자 같은 **가산 효과**로만 갈린다. 16장 대조 시트에서 R 카드가 흰 판처럼 튀는 현상은 사라졌다.

### 12.2 머리 크기 (11절 문제 ②) — 실측 결론

프롬프트로는 **못 맞춘다.** 9절에서 미디엄 샷 문구를 맨 앞에 놓아도, 점프 포즈에서는
"발이 바닥에서 확실히 떨어져 있다" 같은 액션 문장이 프레이밍 지시를 이긴다. 실측:

| 배치 | 프롬프트 | 생성 결과의 머리 크기 |
|---|---|---|
| p002~p006 (배치 1) | 미디엄 샷 문구 있음 | 12~14% |
| p008~p015 (배치 2) | 같은 문구 + 낮은 자세 카메라 문장 | 11~15% |

규격은 18~23%(4.2)이고, 그래야 96px 리스트에서 얼굴이 읽힌다.
그래서 **생성으로 안 되는 것을 크롭으로 맞춘다** — `art-import.mjs --headfrac 0.20`.
16명 전원을 이 값으로 다시 잘라 머리 20% · 얼굴 (50%, 30%)로 통일했다.

**대가**: 세로로 잘려 나가는 만큼 공이 프레임 밖으로 나가는 카드가 생긴다(p010·p012·p015).
카드의 일은 캐릭터를 읽히게 하는 것이고 공은 전신(hero) 그림이 맡는다고 보고 받아들였다.
근본 해결은 **생성 단계에서 더 좁게 뽑는 것**이고, 그건 프롬프트가 아니라 모델의 문제다.

### 12.3 얼굴 좌표는 눈대중으로 재면 틀린다

`--face` 없이 넣으면 4.2 권장 위치(50%, 30%)를 가정하는데 **16장 중 11장이 빗나갔다.**
대조 시트를 보고 손으로 재는 방식도 두 번 다 틀렸다 — 격자 시트에서 읽은 %가
패널 원점이 아니라 이미지 전체 기준이 되기 쉽다.

그래서 `meta.json` 의 `source.crop` 을 이용해 **카드 좌표계에서 읽은 %를 원본 좌표로 되돌리는**
절차로 바꿨다. 카드 위에 지금 좌표가 만드는 썸네일 정사각형과 얼굴 상자를 그려 놓고
어긋난 만큼만 고치면 한두 번에 수렴한다. 16명 전원 44px 초상에서 얼굴이 잡힌다.

> 크로미움 `FaceDetector` 는 이 환경에서 쓸 수 없다(`--enable-features=ShapeDetection` 을 줘도 없다).
> 자동 검출이 되면 이 절은 통째로 사라진다.

### 12.4 남은 것

| # | 항목 | 상태 |
|---|---|---|
| 1 | p012 국다율 — 데이터는 `로우 트윈테일`인데 풀린 긴 머리로 나왔다 | **해결**(13절). 재생성 |
| 2 | p010 채보름 · p038 류별하 — 바닥 나무색이 다른 카드보다 밝다 | 경미. 12.1 범위 안 |
| 3 | 전신(hero) 그림은 p001 한 장뿐 | 카드 42장을 끝냈으므로 다음 작업 |

**측정 리비전**: 16명 시점 `web/dist/bloom.html` 3409KB(아트 2.14MB, 1인당 평균 137KB,
42명 추정 5.6MB / 예산 11MB).

## 13. 카드 42장 완성 (v1.0)

런칭 로스터 42명 전원의 카드 일러스트가 들어갔다. 전부 같은 모델(`seedream_v5_pro`)·
같은 설정(3:4 · 2k)·같은 프롬프트 뼈대로 뽑고, 전원 `--headfrac 0.20` 으로 잘랐다.

| 항목 | 값 |
|---|---|
| 카드 | 42/42 · 머리 20% · 얼굴 (50±1%, 30±1%) — 42장 전원 4.2 규격 안 |
| 썸네일 | 42/42 · 44px 원형에서 얼굴이 읽힌다 |
| 인라인 용량 | 5.78MB (1인당 평균 141KB) / 예산 11MB |
| 빌드 | `web/dist/bloom.html` 8376KB — 아티팩트 상한 16MB 안 |

**구단 식별**: 여섯 구단이 유니폼 색만으로 갈린다 — 가온 바이올렛 · 해솔 네이비 ·
태령 다크그린 · 적동 크림슨 · 연화 앤티크 골드 · 라온 코럴 오렌지.
리베로 6명(p007·p008·p021·p028·p033·p042)은 전부 팀 대비색이라 카드만 봐도 리베로다(3.2⑤).

**헤어 재생성 1건**: p012 국다율은 `로우 트윈테일`이 풀린 머리로 나와 다시 뽑았다.
편집 패스로는 머리 구조를 못 고친다(8.1). 프롬프트에 **부정형까지** 적으니 잡혔다 —
"두 갈래로 나뉜 낮은 트윈테일, 한쪽에 하나씩, 귀 뒤 아래에서 묶음. 풀린 머리도 아니고
포니테일도 아니고 하이 트윈테일도 아니다." 헤어스타일이 어긋난 카드는 이 방식으로 고친다.

### 13.1 42장에서 확인된 것

- **화풍 편차 없음.** 6개 구단·5개 포지션·4개 등급이 한 화풍으로 읽힌다(8.2 편차 QA 확대판).
- **배경 명도**(12.1)가 42장에서도 유지된다. 밝게 튀는 카드는 없다.
  바닥 나무색이 상대적으로 밝은 것이 2장(p010·p038) 있으나 R 카드가 흰 판처럼 튀던 11절 문제와는 다르다.
- **얼굴 좌표 실측 절차**(12.3)가 42장에서 수렴했다. 실측 1회로 규격에 들어간 것이 41장,
  2회 걸린 것이 배치 1의 8장이다(카드 좌표 읽는 기준을 잘못 잡았던 것 — 12.3 참조).

### 13.1.1 하네스가 완성 때문에 못 돌게 됐던 건

`art-test.mjs` 는 "실물 아트가 없는 선수 pid 를 빌려서" 검사용 파일을 넣는 방식이었다.
실물을 덮어쓰지 않으려고 넣은 안전장치였는데(한 번 실제로 썸네일을 날린 적이 있다),
**42명이 다 차자 빌릴 자리가 없어져 하네스가 통째로 안 돌았다.**

선수 id 가 아닌 합성 id(`zzz-arttest`)로 바꿔 해결했다. 뮤테이션으로 검사가 살아 있는지도 확인했다 —
`art-pack` 의 확장자 우선순위를 png 우선으로 뒤집으면 12건 중 1건이 실패한다.

또 화면 검사의 `<img>` MIME 을 `png` 로 못 박고 있어, 실물 webp 아트가 늘자 헛짚었다(12절 커밋).
검사의 뜻은 "SVG 플레이스홀더가 아니라 인라인된 그림이 나온다" 이므로 `png|webp` 를 받는다.

### 13.2 다음

| # | 할 일 |
|---|---|
| 1 | ~~**전신(hero) 그림**~~ — **완료**(14절). 42장 전원 |
| 2 | 6절 열린 이슈 — "선셋 오렌지"가 12색 팔레트에 없다(p025 심노을), 같은 구단 팔레트 충돌 3건 |
| 3 | ~~신인 세대(런타임 생성)의 아트~~ — **완료**(15절). 외형 풀 72종 |

**측정 리비전**: 42명 시점 `web/dist/bloom.html` 8376KB(아트 5.78MB, 1인당 평균 141KB / 예산 11MB).

## 14. 전신(hero) 42장 (v1.1)

선수 상세 화면 상단 배너용 전신 액션 그림을 42명 전원에 넣었다. 카드와 같은 모델·설정·프롬프트 뼈대에서
**프레이밍 문장만** 바꿔 뽑았고(10절), 자르지 않고 3:4 비율만 맞춰 900px 로 넣었다.

| 항목 | 값 |
|---|---|
| 전신 | 42/42 · 900×1200 · 1장 평균 75KB(49~110KB) |
| 인라인 합계 | **8.80MB** (카드+썸네일 5.78 + 전신 3.02) / 예산 11MB |
| 빌드 | `web/dist/bloom.html` 12,501KB — 아티팩트 상한 16MB 안, 여유 3.5MB |

**생성기 수정 1건**: 전신 프레이밍 문장이 "raised hand … airborne" 하나뿐이라 리베로·세터의
디그·세트 자세와 모순이었다. 낮은 자세용 문장(`FRAMING.heroLow` — "머리 꼭대기부터 바닥에 넓게 디딘
두 발까지")을 따로 두고 `LOW_STANCE` 포지션이면 그쪽을 쓴다. 결과: L·S 12장이 모두 바닥에 붙은
자세로 나왔다(1절의 규칙대로 고칠 곳은 생성기다 — 프롬프트 파일을 손으로 고치지 않았다).

**남은 편차**(경미, 재생성 대상 아님): 공이 규격 색(민트·코럴)이 아니라 파랑·노랑으로 나온 것 2장(p027·p041).
p016 석누리는 카드·전신 모두 `하이 번`이 반묶음으로 나온다 — 13절의 부정형 프롬프트로 다음 리터치 때 다시 뽑는다.

**측정 리비전**: 42명 전신 시점 `web/dist/bloom.html` 12,501KB(아트 8.80MB, 1인당 평균 214KB / 예산 11MB).

## 15. 신인 세대 아트 — 외형 풀 72종 (v1.2)

신인은 런타임 생성이라 저장소에 pid 가 없고, 웹 프로토타입은 단일 HTML 이라 그림을 인라인해야 한다.
그래서 6절의 두 안 중 ②를 택했다 — **외형을 자유 조합하지 않고 미리 그린 풀에서 뽑는다.**

### 15.1 설계

| 항목 | 결정 | 이유 |
|---|---|---|
| 풀 | `ROOKIE_LOOKS` 72종 (`web/engine/rookies.js`) — (헤어스타일, 헤어 컬러, 눈 색, 피부톤) | 신인 카드는 `card.look` 으로 하나를 가리킨다. 그림 키 = `art/04_export/rk01~rk72` |
| 의상 | **중립 연습복(회색)** | 풀 하나가 6구단을 다 받아야 한다. 구단 유니폼이면 6배가 든다 |
| 포즈 | **서브 리시브 대기 자세**, 공 없음 | 어느 포지션의 신인에도 붙어야 한다. 스파이크·세트 같은 포지션 포즈는 못 쓴다 |
| 조명 | R 등급(단일 조명) 고정 | 신인 카드의 희귀도는 프레임이 말한다 |
| 변주 | 카메라 좌우·표정 3종을 인덱스로 돌린다 | 72장이 같은 자세라 지루해지는 것을 막는다 |
| 크기 | 600px · q0.80 · 1장 평균 37KB | 선수 카드(900px)보다 작다 — R 위주 제네릭이고 예산이 빠듯하다 |

**규칙은 자유 조합 때와 같다** (world.md 6.2): (스타일|컬러) 조합은 활동 선수 사이에서 유일, 구단 안 헤어 컬러 유일,
파스텔·원색은 구단당 2명. 단계별로 조건을 풀며 고른다(`makeRookie` 의 tiers) — ① 안 쓴 look + 스타일도 안 씀 +
구단 컬러 규칙 → ② 안 쓴 look + 구단 컬러 규칙 → ③ 안 쓴 look → ④ 같은 구단만 아니면 → ⑤ 아무거나.

**풀 크기는 실측으로 정했다.** 36 → 시즌 11 부터 규칙이 깨짐. 60 → 시즌 13 부터 구단 컬러·파스텔 규칙이 깨짐
(활동 신인 수는 60 이하인데도 — 구단 컬러 제약이 먼저 소진된다). **72 → 시드 6종 × 20시즌에서 위반 0.**
parity 8.2 가 이것을 검사한다(풀이 덮는 동안은 옛 규칙 전부, 소진 뒤에는 "같은 구단에 같은 그림 없음").

이 블록은 `rngLook` 만 쓴다 — 뽑는 횟수가 바뀌어도 능력치 스트림에 닿지 않는다.
parity 전 항목·season-check 가 그대로 통과했다(캘리브레이션 불변).

### 15.2 썸네일 파일을 없앴다 — 초상은 카드에서 오린다

풀 72종에 카드+썸네일을 다 넣으면 예산(11MB)을 넘긴다. 그런데 카드는 이미 4.2 규격(얼굴 (50%, 30%) · 머리 20%)으로
잘려 있으므로 **썸네일은 카드의 정해진 영역을 CSS 로 오리면 된다.** `art-pack` 이 `meta.json` 의 얼굴 상자를
`f:[cx, cy, h]` 로 싣고, 앱(`artThumb`)과 대조 시트(`thumbImg`)가 같은 계산으로 4.3 규격(2.2h 정사각형)을 오린다.
런칭 42명의 `_thumb.webp` 도 지웠다 — **1.3MB 절약**, 파일도 한 종류 준다.

### 15.3 수치

| 항목 | 값 |
|---|---|
| 신인 풀 | 72/72 · 600×800 · 합계 2.65MB |
| 선수 42명 | 카드+전신 7.10MB (썸네일 파일 제거 후) |
| 인라인 합계 | **9.75MB** / 예산 11MB |
| 빌드 | `web/dist/bloom.html` 13,820KB — 아티팩트 상한 16MB, 여유 2.2MB |

### 15.4 얼굴 좌표 72장

12.3 의 절차 그대로 — 6장씩 격자 시트에서 읽어 `meta.json` → 원본 좌표로 되돌려 넣었다(12회).
피부색 검출 자동화를 시도했으나 **민소매 연습복에서 어깨·팔 피부가 얼굴로 잡혀** 폐기했다.
대기 자세는 머리가 프레임의 24~30% 로 크게 나와(규격 18~23% 초과) `--headfrac 0.20` 이 확대할 수 없다 —
전신 프레임 그대로 쓰고 얼굴 위치만 기록했다. 44px 초상은 72장 모두 얼굴이 잡힌다.

### 15.5 남은 것

- 신인 카드에는 전신(hero)이 없다 — 선수 상세에서는 카드를 대신 쓴다. 필요하면 풀 72종의 전신을 같은 방식으로 추가한다(약 2.7MB).
- 시즌 18+ 에는 풀이 다 차서 다른 구단끼리 같은 그림이 생긴다(같은 구단 안에서는 생기지 않는다).

---

## 16. 화면 활용 — 그림을 어디에 쓰는가 (v1.3 · 16.6 = v1.5 · 16.7 = v1.6 · 16.8 = v1.7)

114장(카드 42 · 전신 42 · 신인 풀 72)을 만들고 보니 화면에서 쓰는 방식이 셋뿐이었다 — 28~88px 원형 초상,
선수 상세의 전신 배너, 전신이 없을 때의 작은 카드. 가장 오래 보는 **경기 화면**과 가챠의 정점인
**스카우트 결과**에 그림이 한 장도 없었다. v1.3 은 새 그림 없이 있는 그림의 쓰임을 늘린 것이다.

| 화면 | 전 | 후 | 코드 |
|---|---|---|---|
| 스카우트 단발 | 96px 카드 + 텍스트 | **실물 크기 카드 뒤집기**(등급 프레임, SSR 광, NEW/중복 태그) | `scoutResult` · `cardFace` |
| 스카우트 10연 | 28px 원 + 이름 목록 | **2열 카드 그리드**, 0.13초 간격으로 차례 뒤집기. 카드를 누르면 도감 상세 | `pullResults` |
| 도감 (신설) | 없음 | 런칭 42 + 데뷔한 신인 전부, 3열. 미보유는 흑백 실루엣. 구단·보유만 필터. 상세에 소개·성격·스킬·리포트 구매 | `vAlbum` · `vDexFocus` |
| 경기 인트로 (신설) | 없음 | 양 팀 선발 7명 얼굴을 마주 세운다. 2초 뒤 또는 탭하면 시작 | `showIntro` |
| 경기 컷인 (신설) | 중앙 "득점/실점" 글자 | 랠리를 끝낸 득점(에이스·강타·블로킹·블록 아웃)과 **스킬 발동** 순간에 그 선수의 **상반신**이 옆에서 들어온다. 4배속에서는 생략 | `showCut` · `artBust` |
| 코트 스틱맨 | 머리 = 점 | 공을 만진 선수는 머리 자리에 **64px 원형 얼굴** | `faceBitmap` · `court-render.js faceOf` |
| 랠리 칩 | 글자만 | 22px 초상 + 글자 (칩의 `.act` 가 훈련 버튼 `.act` 스타일을 물려받아 56px 상자로 커지던 버그도 `.tact` 로 고쳤다) | `rallyStrip` |
| 감독실 | 없음 | 간판 선수 전신 상단을 헤더 배경으로(오른쪽, 마스크로 페이드) · 도감 N/M 링크 | `vHome` |
| 로스터·라인업·경기 | 연습생 = SVG 플레이스홀더 | **연습생·결원 대체·영입·구단 신인도 신인 외형 풀의 얼굴**(16.2) | `assignGeneratedLook` |

### 16.1 카드 한 장에서 오려 쓴다

파일은 그대로 카드 하나다. 얼굴 상자 `f=[cx,cy,h]` 를 기준으로 영역만 달리 잡는다(`artCrop(p, alt, kH, dy, aspect)`).

| 용도 | 영역 높이 | 중심 | 비율 |
|---|---|---|---|
| 원형 초상(목록·칩·인트로) | 2.2h | (cx, cy+0.15h) | 1:1 |
| 상반신(컷인) | 3.4h | (cx, cy+0.7h) | 4:5 |
| 코트 얼굴 마커 | 2.2h | 위와 같음 | 64px 캔버스 비트맵 |

**디코드.** 900×1200 카드 10장은 디코드에 초 단위가 걸려 그냥 그리면 뒷면이 **빈 앞면**으로 뒤집힌다.
`preloadArt` 가 `Image.decode()` 로 먼저 디코드하는 동안 카드는 뒷면(`.rcard.pend`)으로 놓여 있고, 끝나면 뒤집는다(최대 2.5초).
코트 얼굴은 선수당 한 번만 카드를 디코드해 64px 캔버스로 잘라 두고 프레임마다 그 비트맵만 그린다(`FACE_BMP`).
도감의 42+장은 `loading="lazy"` 다.

### 16.2 생성 선수의 얼굴 — 스탯 RNG 를 건드리지 않는다

연습생 7명, 결원 대체(육성 선수·영입), 구단 자체 신인은 매번 시드에서 다시 만드는 선수라 카드가 없다.
`assignGeneratedLook(p, taken)` 이 **id 해시**로 외형 풀 72종에서 그림을 고른다 — RNG 를 소비하지 않으므로
스탯·세이브·캘리브레이션은 비트 단위로 같다(`parity.mjs` · `season-check.mjs` 출력이 시간 항목만 빼고 기준 커밋과 동일).
피하는 것: 그 시즌까지 데뷔한 신인 카드의 그림(스카우트해 온 신인과 연습생이 같은 얼굴이 되지 않게), 같은 묶음(연습생 7명 · 한 구단 로스터) 안의 중복.
시즌 1~3 에는 신인이 없으므로 제외 집합이 비어 있다.

함께 고친 것: `cardById` 가 캐시된 신인 세계를 `state.season` 까지 자라게 하지 않아, 세이브 복원(시즌 ≥ 4)에서
신인 카드 인스턴스가 사라지는 회귀가 있었다(`season-check` "신인 카드 인스턴스가 저장→복원된다"). 이제 항상 `rookieWorld(state)` 를 거친다.

### 16.3 검증

`node web/app-test.mjs` 66건(53 → 66): 실물 카드 공개 · 10연 그리드 · 도감 42장/실루엣/필터 · 연습생 초상이 그림 ·
인트로 얼굴 14 · 인트로 탭 · 2배속에서 컷인과 랠리 칩 초상. `art-test` 12건, `parity`, `season-check` 그대로.

### 16.4 두 번째 묶음 (v1.4) — 선수 표현 계층과 같이

| 화면 | 후 | 코드 |
|---|---|---|
| 육성 캠프 헤더 | 36px 원 → **상반신** + 성격 아키타입 배지·태그 3개. 치료 턴엔 붉은 틴트 | `vTrainTurn` · `tagRow` · `bustBox` |
| 스토리·선배 이벤트 | 비주얼노벨식 — 컷 왼쪽에 그 선수(또는 선배)의 상반신, 텍스트는 아키타입별 | `.evbox.vn` |
| 졸업 | 실물 카드 + 등급 도장(회전 원형 스탬프) | `vTrainGrad` · `.stamp` |
| 결산 MVP | 전신 상단 16:9 크롭 배너(없으면 카드) — AI 구단 결원 대체·신인도 구단 로스터에서 찾아 그린다 | `mvpBlock` |
| 은퇴 · 명예의 전당 | 전당 0단계: 흑백 초상 행. 1단계 이상: **세피아 카드 벽**(시설의 보이는 보상) | `retireBlock` |
| 신인 드래프트 | 2열 카드 공개(차례 뒤집기) → 도감 상세 | `rookieBlock` |
| 상태 필터 | 하락기 채도↓ · 은퇴 흑백 · 전성기 금테 (CSS 필터, 그림 추가 없음) | `phaseClass` |
| 경기 컷인 | 부제가 이름 대신 **성격 양념 문장**(클러치 강타·에이스·블로킹) | `showCut` |
| 경기 인트로·매치데이 카드 | 경기 전 서사 줄(친정·숙적·선두·전적) | `matchStory` |

결산의 "우승!" 표시가 `champion`(우승 구단 id)을 보고 있어 남이 우승해도 떴다 — `isChampion` 으로 고쳤다.

### 16.5 남은 것

- 신인 전신이 없어 인트로·컷인·MVP 배너에서 카드 상반신을 쓴다(15.5).
- 인연 **보너스**(스탯·케미)는 서사만 있고 효과가 없다 — training-mode 6.4.

### 16.6 육성→코트 연결 연출 (v1.5) — "캠프에서 고른 것이 코트에서 보인다"

이 게임에서 플레이어가 실제로 결정을 내리는 곳은 캠프뿐이고, 그 결정의 결과는 경기에서 나온다. 둘을 잇는 연출이 없으면
캠프는 숫자 놀음이고 경기는 구경이 된다. 그래서 경기의 기록을 **"캠프에서 키운 그 능력치"** 에 직접 붙인다. 전부 표현 계층이다 —
엔진은 박스스코어를 읽어 통산 기록(`state.career`)을 쌓을 뿐 판정·RNG 를 건드리지 않는다(`recordCareer` · `campTrace`, season-check 불변식 6건).

| 자리 | 연출 | 데이터 |
|---|---|---|
| 경기 인트로 | 오늘 처음 코트에 서는 졸업생에게 **데뷔** 배지 + "오늘 데뷔전: ○○ — 캠프에서 키운 것이 코트에서 보이는 첫날" | `view.career.debut` |
| 코트 | 데뷔전 선수의 얼굴 마커에 금색 링(`R.markOf`), 랠리 칩 테두리 | 같음 |
| 득점 컷인 자막 | 경기당 (선수, 능력치) 한 번: **"손라희 · 캠프에서 블로킹 66→88"**. 데뷔전 선수의 첫 득점은 "데뷔전 첫 득점". 그다음부터 성격 대사 | `inst.initialStats/finalStats` |
| 경기 결과 | ① **데뷔전 카드**(얼굴·오늘 기록·"캠프에서 스파이크 68→87 (+19) — 그게 오늘 공격 득점 7이 됐다") ② **캠프의 흔적**(졸업생 상위 3명: 오늘 한 일 ← 캠프 능력치) ③ 🎉 통산 첫 기록(첫 득점·첫 블로킹·첫 에이스·첫 두 자릿수·10경기·100득점) | `campTrace` · `view.career.firsts` |
| 선수 상세 | 통산 기록(경기·득점·내역·데뷔전 상대와 결과). 경기 전이면 "다음 경기가 데뷔전" | `careerOf` |
| 졸업 화면 | 라인업에 서게 되면 버튼이 **"데뷔전 치르러 가기"** | `myTeamState` |

`campTrace` 는 오늘 한 일이 큰 순서(공격 득점→스파이크, 블로킹→블로킹, 에이스→서브, 디그→디그)로 보고 캠프 상승이 5 미만이면 다음 항목으로 넘어간다.
연결할 것이 없으면 연출을 띄우지 않는다 — 억지로 잇는 순간 거짓말이 된다.

### 16.7 경기 화면 연출 1단계 (v1.6) — "선과 선"을 벗어난다

테스터의 말: "이미 너무 선과 선으로 이루어져서 누가 보더라도 초기 모델 같다." 원인은 선수가 선이고, 공이 점이고, 카메라가 안 움직이는 것.
새 그림 없이 캔버스 렌더러(`web/court-render.js`)와 뷰어 DOM 만으로 고쳤다. 전부 표현 계층이며 판정·RNG 와 무관하다.

| 자리 | 전 | 후 |
|---|---|---|
| 선수 | 막대 인간(선 5개) | 유니폼 색으로 채운 실루엣(반바지·몸통·팔) + 등번호 + 얼굴 비트맵 + 그림자. 터치 종류별 자세(서브·토스·강타 스윙 잔상·블로킹·디그 플랫폼), 다음 터치 예정자는 준비 자세(블로커는 공이 오면 뛴다), 대기 선수는 숨쉬기 |
| 공 | 점 + 얇은 궤적 | 굵기·투명도가 커지는 잔상(강타·서브는 길고 밝게), 높이에 따라 벌어지는 그림자, 착지 링 + 큰 득점은 두 겹 링·섬광·먼지 |
| 카메라 | 고정 | 강타가 날아가는 동안 1.13배 줌(타자↔낙하점 중간을 축으로), 공을 따라 미세 팬, 강타·착지 흔들림 |
| 시간 | 일정 | **느려지는 건 때리기 전, 공은 빠르다.** 큰 득점(에이스·강타·블로킹·클러치)이나 스킬 발동 **직전** 비행의 마지막 35% 를 0.3배(예고 — 때릴 선수에게 1.2배 줌, 발밑 금색 링, 스킬명), 때리는 순간 0.09초 히트스톱(섬광·방사선), 마무리 비행은 1.35배로 꽂히고 바로 플래시. 에이스·스킬 서브는 서브 전 0.55초 숨 고르기. 4배속은 전부 생략. (첫 판은 마지막 비행 후반을 0.42배로 늘였는데 "공이 오히려 느려 보인다"는 테스터 피드백으로 뒤집었다) |
| 득점 표시 | "득점/실점" | 사유별 ACE · KILL · WALL · BLOCK OUT · 범실 + 한 줄 설명, 클러치 접두, 3연속 득점 배지, 점수 숫자 팝 |
| 경기장 | 파란 사각형 | 마루 프리존(널 결) → 코트 스포트라이트·네트 앞 광택, 천장 조명 세 개, 계단식 관중석(기본 밀도 35% + 시설 등급), 구단 색 현수막 띠 |
| 관중 | 정지 점 | 우리 득점에 들썩임 + 휴대폰 플래시, 상대 득점에 잠깐 조용 (`R.cheerFor(side)`) |
| 네트 | 고정 | 공이 걸리면 출렁임 |
| 선수 이동 | 자기 자리에 고정, 접점과 무관하게 서 있음 | 비행마다 이동 계획(`planFlight`): 다음에 만질 선수는 접점으로 달려가 공보다 먼저 도착(80%), 방금 친 선수는 잠시 뒤 자리로, 강타는 네트 앞 도움닫기, 서버는 엔드라인 뒤, 다다음이 블로킹이면 블로커가 공격수 앞 네트로 미리 옮겨 서고 이웃 전위가 반쯤 따라감(더블 블록), 나머지는 공의 낙하 방향으로 살짝 쏠림. 달릴 때 다리·팔이 엇갈린다 |
| 세트·매치 포인트 | 없음 | 코트 가장자리가 뜨겁게 뛰고 위에 "세트 포인트 / 매치 포인트 / 듀스" 태그 |
| 세트 종료 | 토스트 | "1세트 종료 · 25-21 · ○○ 세트 획득" 배너가 쓸고 지나간다 |

2단계(그림 추가)는 16.8 — 런칭 42명 전신을 배경 제거해 아크릴 스탠드처럼 세웠다.

### 16.8 경기 화면 연출 2단계 (v1.7) — 스탠디: 전신 컷아웃이 코트에 선다

1단계 실루엣의 한계는 **코트에 얼굴이 없다**는 것이었다. 42명 카드·전신이 있는데 경기 중에는 유니폼색 도형만 보였다.
2단계는 새 그림 없이 전신(hero) 42장의 배경을 지워 **아크릴 스탠드처럼 코트에 세운다.** 렌더러는 그림 한 장을 발 앵커에
놓고, 방향을 뒤집고, 스쿼시·스트레치·기울임으로 움직임을 붙인다. 전부 표현 계층이며 판정·RNG 와 무관하다.

**파이프라인** — `python3 tools/art-standee.py` (pillow·numpy·scipy·opencv·rembg·onnxruntime)

| 단계 | 하는 일 | 왜 |
|---|---|---|
| ① 배경 제거 | rembg `isnet-anime` (애니메 선화·머리카락용 모델) | u2net 보다 머리카락 가장자리가 또렷하다(p001 비교) |
| ② 손 지정 사각형 | `standee_manual.cuts` 안의 알파를 지운다 | 네트 줄처럼 몸 밖으로 뻗은 배경 조각(p006) |
| ③ 공 | 허프 원 + 색 구성(흰 바탕 ≥30% · 청록/살구 패널 ≥5%)으로 공을 찾고, 둘레의 몸 접촉이 50% 이하면 잘라낸다 | 코트에서는 렌더러가 진짜 공을 그린다. 몸 앞을 가린 공은 잘라내면 구멍이 남아 그대로 둔다 |
| ④ 성분 | 가장 큰 연결 성분(선수)만 남기고, 떨어진 공·부스러기는 버린다 | 배경 제거가 남긴 조각 |
| ⑤ 색 번짐 | 반투명 가장자리 픽셀의 색을 가장 가까운 불투명 픽셀 색으로 덮는다 | 어두운 배경색이 머리카락 테두리에 묻어 나오는 것 |
| ⑥ 축소 | 몸 상자로 자르고 세로 **320px** (프리멀티플라이 후 LANCZOS) | 코트에서 최대 ~160px(2x)이라 충분. 42장 0.9MB |
| ⑦ 앵커 | 알파 질량의 세로 분포 3%·97% = 몸 구간, 맨 아래 질량의 x = 발, 맨 위 질량의 x = 머리 | 팔을 뻗어도 몸 구간이 흔들리지 않는다 |
| ⑧ 방향 | 공이 선수 질량 중심의 오른쪽이면 R, 왼쪽이면 L, 가운데면 F. `standee_manual.facing` 이 우선 | 렌더러가 네트 쪽을 보게 뒤집는 기준. 애니메 얼굴 검출기(lbpcascade)는 42장 중 17장만 잡아 쓰지 않았다 |

출력은 `art/04_export/{pid}/{pid}_stand.webp` + `{pid}_meta.json` 의 `standee`(w·h·foot·head·top·bottom·facing·ball·crop·model).
사람이 정한 값은 `standee_manual`(facing·model·cuts·ball) 에 두고 도구는 읽기만 한다 — `--set p006 model=isnet-general-use facing=F 'cuts=[[0,360,312,65]]'`.
`--check` 는 hero 가 있는데 stand 가 없거나 앵커가 빠지면 실패한다(CI 게이트). `--preview` 는 42장을 한 장에 모아 앵커를 그려 준다.

**42장 결과** (v1.7 측정, 합계 0.88MB · 평균 21KB): 모델 교체 2장(p006 네트 줄 → `isnet-general-use` + cuts 4개, p042 바닥 → `isnet-general-use`).
방향은 42장 전부 눈으로 확정해 `standee_manual.facing` 에 적었다(공 추정은 10장에서만 나왔고 정면·측면 구분이 약하다).

| 공 처리 | 장수 | 뜻 |
|---|---|---|
| cut | 19 | 손끝에 걸친 공(둘레 접촉 ≤ 50%)을 원판으로 잘라냈다 |
| attached | 12 | 몸 앞을 가린 공(접촉 > 50%) — 잘라내면 구멍이라 남겼다. 코트에서 "공을 든 스탠디"로 보인다 |
| removed | 2 | 떨어져 있던 공을 성분 분석에서 버렸다 |
| none | 9 | 공이 없거나 못 찾았다 |

**렌더러** (`web/court-render.js drawStandee`) — 그림 내용은 모르고 앵커 `sm = [발x, 발y, 몸위, 몸아래, 머리x, 머리y, 방향]` 만 읽는다.

| 규칙 | 값 |
|---|---|
| 크기 | 몸 구간(위~아래)이 **2.35 × s** (s = 코트 1m 의 화면 px, 390px 폰에서 ≈21) → 몸 ≈ 49px, 2x 에서 98px. 포즈가 달라도 선수 크기가 비슷하다 |
| 방향 | 기본은 네트 쪽(`project` 로 네트 방향의 화면 x 부호를 구해 뒤집기), 달리는 중이면 달리는 쪽 |
| 자세 | 강타 순간 1.12배 펀치 → 점프 세로 +10% / 가로 −6% → 리시브·디그 세로 0.86 / 가로 1.08 → 달리기 14° 기울임 + 들썩임 → 대기 숨쉬기 ±1.2% → 예고 중 펄스 |
| 팀색 림 | 팀색으로 칠한 실루엣을 여덟 방향(0.055 × s)으로 밀어 그린다 — 아크릴 테두리 + 팀 구분. 유니폼 색은 그림의 구단색 그대로 |
| 이름·스킬·데뷔 링 | 머리 앵커 위. 데뷔 링은 머리 둘레 |
| 없을 때 | `standOf` 가 null 이면 1단계 실루엣 그대로 |

**12명 전원이 선다.** 1단계는 공을 만진 선수만 신원을 알았다(로그의 터치 이벤트). 엔진이 `RallyStart` 이벤트에 `lineup`(리베로 교대
반영, 자리 1~6 순 12명 id)을 붙이고(`match.js`, 판정·RNG 무관, parity ✅), 뷰어가 `R.lineup` 으로 넘긴다.
**대역(ghost)**: 누끼가 없는 선수(연습생·신인 풀 72종은 카드만 있다·결원 대체)는 기증자 5명(p029·p012·p024·p025·p020, 공 없는 또렷한 자세)의
윤곽을 어두운 팀색으로 칠하고 얼굴 원형(카드에서 오린 것)을 얹는다. 선수 id 해시로 기증자를 고정한다. 그래야 코트 위 12명이 같은 크기·같은 어법이다.

**검증**: `node web/art-test.mjs` 16건 — 팩이 s·sm 을 싣는가, 세로 320 규격, 경기 화면에서 프레임당 스탠디 12명(실물 누끼가 하나도 없으면 0 = 실루엣 폴백), 저장소 원복.
`python3 tools/art-standee.py --check`. 빌드 `web/dist/bloom.html` 13.6MB → **14.8MB** (아티팩트 상한 16MB — 다음 그림을 넣으려면 hero 42장(4.1MB)을 줄여야 한다).

**한계 → 3단계**: 그림이 한 장뿐이라 자세는 변형으로만 낸다(점프하는 그림이 리시브 자세로 찌그러진다). 몸 앞을 가린 공은 남아 있다.
제대로 하려면 공용 바디 2~3종을 리깅(Spine·Unity 2D Animation)하고 얼굴·머리·유니폼만 갈아 끼우는 3단계(GDD 13절)로 간다.

