{
  "name": "3sec",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter viewer dev",
    "build": "pnpm -r build",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "typecheck": "pnpm -r typecheck",
    "sim": "pnpm --filter @3sec/engine sim"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
